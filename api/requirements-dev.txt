# Test-only dependencies. NOT installed on the droplet.
#
# api/requirements.txt is the production install; keep it free of test tooling.
# The offline suite needs these two: pytest.ini sets asyncio_mode = auto, which
# is a pytest-asyncio setting, so without pytest-asyncio the async tests in
# test_schwab_client.py error out on a venv built strictly from requirements.txt.
#
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
