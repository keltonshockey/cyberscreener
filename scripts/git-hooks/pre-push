#!/bin/sh
# Pre-push gate: the offline backend suite must be green before anything
# leaves this clone. Install once per clone with: make install-hooks
# Bypass for a true emergency with: git push --no-verify
set -e
echo "pre-push: running backend test suite (make test)..."
make -C "$(git rev-parse --show-toplevel)" test
